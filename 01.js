/**
 * @param {number[]} array
 * @returns {number|undefined} Largest number, or undefined if array is empty
 */
const getLargestNumber = (array) => {
  //TODO abcd
};

module.exports = {
  getLargestNumber,
};
