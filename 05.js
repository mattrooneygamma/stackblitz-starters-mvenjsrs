/**
 * Given an object, returns the object with keys sorted by the value low to high
 * @param {{[key: string]: number}} object
 * @returns {{[key: string]: number}}
 * @example
 *    const scores = { megan: 70, harry: 60, elizabeth: 90, charles: 50 }
 *    sortObjectKeysByValue(scores) // { charles: 50, harry: 60, megan: 70, elizabeth: 90 }
 */
const sortObjectKeysByValue = (object) => {
  /* TODO */
};

module.exports = {
  sortObjectKeysByValue,
};
